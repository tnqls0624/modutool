# 🌐 Firebase 도메인 승인 가이드

Firebase에서 "현재 도메인이 승인되지 않았습니다" 오류가 발생할 때 해결 방법입니다.

---

## 🚨 오류 메시지

```
auth/unauthorized-domain
현재 도메인이 승인되지 않았습니다.
```

이 오류는 Firebase가 현재 웹사이트의 도메인을 신뢰하지 않을 때 발생합니다.

---

## ✅ 해결 방법 (단계별)

### Step 1: Firebase Console 접속

1. **[Firebase Console](https://console.firebase.google.com/)** 접속
2. 본인의 프로젝트 선택

### Step 2: Authentication 메뉴 이동

1. 왼쪽 메뉴에서 **"빌드"** 섹션 찾기
2. **"Authentication"** 클릭

### Step 3: Settings (설정) 탭 이동

1. 상단 탭에서 **"Settings"** (설정) 클릭
   - 한국어 콘솔: **"설정"** 탭
   - 영어 콘솔: **"Settings"** 탭

### Step 4: 승인된 도메인 섹션 찾기

페이지를 아래로 스크롤하여 **"Authorized domains"** (승인된 도메인) 섹션 찾기

**기본적으로 포함된 도메인:**
- `localhost` (로컬 개발용)
- `your-project-id.firebaseapp.com` (Firebase Hosting)
- `your-project-id.web.app` (Firebase Hosting)

### Step 5: 도메인 추가

1. **"Add domain"** (도메인 추가) 버튼 클릭

2. 팝업 창에서 현재 사용 중인 도메인 입력

   **로컬 개발 중인 경우:**
   ```
   localhost
   ```
   또는 포트 포함:
   ```
   localhost:8000
   localhost:5500
   ```

   **127.0.0.1 사용하는 경우:**
   ```
   127.0.0.1
   127.0.0.1:8000
   ```

   **실제 도메인 사용하는 경우:**
   ```
   mysite.com
   www.mysite.com
   ```

3. **"Add"** (추가) 버튼 클릭

### Step 6: 확인

- 추가한 도메인이 "Authorized domains" 목록에 표시되는지 확인
- ✅ 표시되면 성공!

### Step 7: 테스트

1. 브라우저에서 **새로고침** (`Ctrl+R` 또는 `F5`)
2. Google 로그인 다시 시도
3. ✅ 로그인 성공!

---

## 📋 상황별 도메인 입력 예시

### 1. VS Code Live Server 사용 중
```
localhost:5500
127.0.0.1:5500
```

### 2. Python HTTP Server 사용 중
```
localhost:8000
127.0.0.1:8000
```

### 3. Node.js HTTP Server 사용 중
```
localhost:3000
127.0.0.1:3000
```

### 4. Netlify에 배포
```
your-site.netlify.app
your-custom-domain.com
```

### 5. Vercel에 배포
```
your-site.vercel.app
your-custom-domain.com
```

### 6. GitHub Pages에 배포
```
username.github.io
```

### 7. 커스텀 도메인 사용
```
mysite.com
www.mysite.com
```

---

## 🖼️ 스크린샷 가이드

### 화면 구성

```
Firebase Console
└── [프로젝트 선택]
    └── Authentication
        └── Settings (설정) 탭
            └── Authorized domains (승인된 도메인)
                ├── localhost ✅ (기본 포함)
                ├── your-project.firebaseapp.com ✅ (기본 포함)
                └── [Add domain] 버튼 ← 여기 클릭!
```

---

## 💡 팁과 주의사항

### 1. 포트 번호 포함 여부

**포트 번호를 명시하는 경우:**
- ✅ 권장: `localhost:8000` (정확한 포트)
- ⚠️ 주의: 포트가 바뀌면 다시 추가해야 함

**포트 번호를 생략하는 경우:**
- ✅ `localhost` (모든 포트 허용)
- 💡 더 편리하지만 보안 측면에서는 명시하는 것이 좋음

### 2. www 포함 여부

커스텀 도메인 사용 시 둘 다 추가하는 것이 좋습니다:
```
mysite.com
www.mysite.com
```

### 3. 프로토콜 (http/https)

❌ **프로토콜은 포함하지 않습니다:**
```
❌ https://mysite.com  (잘못됨)
✅ mysite.com          (올바름)
```

### 4. 경로 포함 안 함

❌ **경로는 포함하지 않습니다:**
```
❌ mysite.com/auth.html  (잘못됨)
✅ mysite.com            (올바름)
```

---

## 🔍 현재 도메인 확인 방법

현재 사용 중인 도메인을 모르는 경우:

### 방법 1: 브라우저 주소창 확인
주소창에 표시된 URL 확인:
```
http://localhost:8000/auth.html
         ↑          ↑
      도메인      포트
```
→ 추가할 도메인: `localhost:8000` 또는 `localhost`

### 방법 2: 브라우저 콘솔에서 확인
1. `F12` 눌러 개발자 도구 열기
2. **Console** 탭 클릭
3. 다음 코드 입력 후 Enter:
```javascript
console.log('현재 도메인:', window.location.host);
```
4. 출력된 값을 Firebase에 추가

### 방법 3: JavaScript로 자동 확인
브라우저 콘솔에서:
```javascript
alert('도메인: ' + window.location.hostname + '\n포트: ' + window.location.port);
```

---

## 🚀 빠른 해결 체크리스트

문제가 계속되면 다음을 확인하세요:

```
□ Firebase Console 접속 완료
□ 올바른 프로젝트 선택
□ Authentication > Settings 이동
□ Authorized domains 섹션 확인
□ 현재 도메인 추가 완료
□ 도메인이 목록에 표시됨
□ 브라우저 새로고침 (Ctrl+R)
□ 캐시 삭제 (Ctrl+Shift+Delete)
□ 시크릿 모드에서 테스트
```

---

## 🆘 여전히 문제가 해결되지 않는 경우

### 1. Firebase 프로젝트 재확인
- 올바른 프로젝트에서 작업 중인지 확인
- Project ID가 `admin.html` 설정과 일치하는지 확인

### 2. 브라우저 캐시 완전 삭제
```
1. Ctrl+Shift+Delete (Mac: Cmd+Shift+Delete)
2. "전체 기간" 선택
3. "캐시된 이미지 및 파일" 체크
4. "쿠키 및 기타 사이트 데이터" 체크
5. "데이터 삭제" 클릭
6. 브라우저 재시작
```

### 3. 시크릿 모드에서 테스트
- Chrome: `Ctrl+Shift+N`
- Firefox: `Ctrl+Shift+P`
- Safari: `Cmd+Shift+N`

### 4. 다른 브라우저에서 테스트
Chrome, Firefox, Safari, Edge 중 다른 브라우저로 시도

### 5. Firebase 설정 재확인
`admin.html` > 설정 탭에서:
- API Key가 올바른지 확인
- Auth Domain이 올바른지 확인
- Project ID가 올바른지 확인

---

## 📝 예제: localhost 추가 과정

### 상황
- 로컬 서버: `http://localhost:8000`
- 오류: "auth/unauthorized-domain"

### 해결 과정

**1단계: Firebase Console 접속**
```
https://console.firebase.google.com/ → 프로젝트 선택
```

**2단계: 메뉴 이동**
```
Authentication → Settings 탭
```

**3단계: 도메인 확인**
```
Authorized domains 섹션:
✅ localhost (이미 있음)
✅ your-project.firebaseapp.com (이미 있음)
```

**4단계: 결과**
- `localhost`가 이미 있으면 → 다른 문제 (Firebase 설정 재확인)
- `localhost`가 없으면 → "Add domain" 클릭하여 추가

---

## 💻 개발 환경별 설정

### Local Development (로컬 개발)
```
추가할 도메인:
✅ localhost
✅ 127.0.0.1
```

### Staging (스테이징)
```
추가할 도메인:
✅ staging.mysite.com
✅ test.mysite.com
```

### Production (프로덕션)
```
추가할 도메인:
✅ mysite.com
✅ www.mysite.com
```

---

## 🔗 추가 리소스

- [Firebase Authentication 문서](https://firebase.google.com/docs/auth)
- [Firebase 보안 규칙](https://firebase.google.com/docs/rules)
- [Firebase 문제 해결](https://firebase.google.com/support/troubleshooter/report/features)

---

## 📞 도움이 필요하신가요?

문제가 계속되면 다음 정보를 확인해주세요:

1. **브라우저 콘솔 에러 메시지** (F12 → Console)
2. **현재 URL** (주소창 전체)
3. **Firebase Project ID** (admin.html 설정에서 확인)
4. **Authorized domains 목록** (Firebase Console에서 확인)

---

**마지막 업데이트**: 2025-01-22  
**버전**: 1.0.0  
**작성자**: AI연구소 개발팀
