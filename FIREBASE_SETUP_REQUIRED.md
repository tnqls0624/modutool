# 🔥 Firebase 설정이 필요합니다

## 현재 문제
로그인/회원가입 버튼이 표시되지 않는 이유는 **Firebase 설정이 완료되지 않았기 때문**입니다.

## ✅ 해결 방법

### 1단계: Firebase Console에서 설정 가져오기

1. **Firebase Console 접속**
   - 🔗 https://console.firebase.google.com/
   - 프로젝트 선택: `modutool-4179c`

2. **프로젝트 설정 열기**
   - 좌측 메뉴에서 ⚙️ (톱니바퀴) 클릭
   - "프로젝트 설정" 선택

3. **웹 앱 설정 찾기**
   - "내 앱" 섹션으로 스크롤
   - 웹 앱 (</> 아이콘) 선택
   - "SDK 설정 및 구성" 선택
   - "구성" 라디오 버튼 선택

4. **설정 복사**
   ```javascript
   const firebaseConfig = {
     apiKey: "AIza...",  // 이 값들을 복사하세요
     authDomain: "modutool-4179c.firebaseapp.com",
     projectId: "modutool-4179c",
     storageBucket: "modutool-4179c.firebasestorage.app",
     messagingSenderId: "123456...",
     appId: "1:123456..."
   };
   ```

### 2단계: 설정 적용 방법 (2가지 옵션)

#### 옵션 A: 관리자 페이지에서 설정 (권장)

1. **배포된 사이트 접속**
   - 🔗 https://modutool-4179c.web.app/admin.html

2. **"설정" 탭 클릭**

3. **Firebase 설정 입력**
   - Firebase Console에서 복사한 값들을 각 필드에 입력
   - "설정 저장" 버튼 클릭

4. **페이지 새로고침**
   - 이제 로그인/회원가입 버튼이 표시됩니다!

#### 옵션 B: 코드에 직접 입력

1. **파일 수정**
   ```bash
   # public/js/firebase-config.js 파일 열기
   ```

2. **30-38번째 줄 수정**
   ```javascript
   // 기본 설정 (수동으로 입력한 경우)
   return {
       apiKey: "여기에_실제_API_KEY",
       authDomain: "modutool-4179c.firebaseapp.com",
       projectId: "modutool-4179c",
       storageBucket: "modutool-4179c.firebasestorage.app",
       messagingSenderId: "여기에_실제_SENDER_ID",
       appId: "여기에_실제_APP_ID"
   };
   ```

3. **재배포**
   ```bash
   firebase deploy --only hosting
   ```

## 🎯 확인 방법

설정이 완료되면:

1. **브라우저 콘솔 확인** (F12)
   - ✅ "Firebase 초기화 성공" 메시지 표시
   - ✅ "📌 Project ID: modutool-4179c" 표시

2. **네비게이션 바 확인**
   - ✅ "로그인" 버튼 표시
   - ✅ "회원가입" 버튼 표시

3. **로그인 페이지 테스트**
   - 🔗 https://modutool-4179c.web.app/auth.html
   - 이메일 로그인 폼이 정상 표시되어야 함

## ⚠️ Firebase Authentication 활성화 필수

설정 후에도 로그인이 안 되면 Firebase Console에서:

1. **Authentication 메뉴 클릭**
2. **Sign-in method 탭 선택**
3. **이메일/비밀번호** 활성화
4. **Google 로그인** 활성화 (선택사항)

## 🔗 현재 배포된 사이트

- 메인: https://modutool-4179c.web.app/
- 관리자: https://modutool-4179c.web.app/admin.html
- 로그인: https://modutool-4179c.web.app/auth.html

---

**💡 팁**: 관리자 페이지(옵션 A)를 사용하면 코드 수정 없이 브라우저에서 바로 설정할 수 있어 편리합니다!
